Role Name,Layer,Supported Architecture,Details
Default - ``default``,Compute,x86_64 and aarch64,"* Omnia assigns the default role to nodes which do not fit into any specific roles.
* This role is useful when you only need to provision the nodes, without assigning any specific functionality to them.

**Example**:
::
    Roles:
      - name: ""default""
        groups:
          - grp0"
Slurm worker node - ``slurm_node``,Compute,x86_64 and aarch64,"* This role is used to configure the nodes for Slurm worker. The nodes included in this role will have the necessary tools and configurations to run Slurm worker.
* The nodes in this role can be used to run Slurm worker.

**Example**:
::
    Roles:
      - name: ""slurm_node""
        groups:
          - grp0
          - grp1"
Kubernetes worker node - ``kube_node``,Compute,x86_64 and aarch64,"* This role is used to configure the nodes for Kubernetes worker. The nodes included in this role will have the necessary tools and configurations to run Kubernetes worker.
* The nodes in this role can be used to run Kubernetes worker.

**Example**:
::
    Roles:
      - name: ""kube_node""
        groups:
          - grp0
          - grp1"
Service Cluster Kubernetes worker node - ``service_kube_node``,Management,x86_64 and aarch64,"* This role is used to configure the Kubernetes worker nodes on service cluster. This role should not have same group as ``kube_node`` role.
* The nodes included in this role will have the necessary tools and configurations to configure and run Kubernetes worker on service cluster.
* ``service_kube_node`` role: Service Cluster Kubernetes worker node groups can be provided in this role.

**Example**:
::
    - name: ""service_kube_node""
      groups:
        - grp0
        - grp1"
Compiler - ``compiler_node``,Management,x86_64 and aarch64,"* This role is used to configure the nodes for compilation. The nodes included in this role will have the necessary tools and configurations to perform compilation.
* The nodes in this role can be used to compile the code.

**Example**:
::
    Roles:
      - name: ""compiler_node""
        groups:
          - grp0
          - grp1"
Slurm control plane - ``slurm_control_node``,Management,x86_64,"* Nodes with ``slurm_control_node`` role can be added to the Slurm head node groups.
* This role is used to configure the nodes for Slurm head. The nodes included in this role will have the necessary tools and configurations to run Slurm head.
* The nodes in this role can be used to run the Slurm head.

**Example**:
::
    Roles:
      - name: ""slurm_control_node""
        groups:
          - grp0
          - grp1"
Login node - ``login_node``,Management,x86_64 and aarch64,"* This role is used to configure the nodes for user logins. The nodes included in this role will have the necessary tools and configurations to support user login activities.

**Example**:
::
    Roles:
      - name: ""login_node""
        groups:
          - grp0
          - grp1"
